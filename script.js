document.addEventListener('DOMContentLoaded',function(){const slides=document.querySelectorAll('.carousel-item');const radioButtons=document.querySelectorAll('input[name="radio-btn"]');let currentSlide=0;const totalSlides=slides.length;function moveToSlide(index){const offset=-index*100;slides.forEach(slide=>{slide.style.transform=`translateX(${offset}%)`})}
function autoSlide(){currentSlide=(currentSlide+1)%totalSlides;moveToSlide(currentSlide);radioButtons[currentSlide].checked=!0}
let slideInterval=setInterval(autoSlide,4000);radioButtons.forEach((radioButton,index)=>{radioButton.addEventListener('change',()=>{clearInterval(slideInterval);currentSlide=index;moveToSlide(currentSlide);slideInterval=setInterval(autoSlide,4000)})});const acc=document.getElementsByClassName("accordion");Array.from(acc).forEach((button)=>{button.addEventListener("click",function(){const pannel=this.nextElementSibling;const img=this.querySelector('img');if(pannel.style.display==="block"){pannel.style.display="none";this.classList.remove("active");this.parentElement.classList.remove("active");img.src='/assets/images/Down.svg'}else{Array.from(document.querySelectorAll('.pannel')).forEach(panel=>{if(panel!==pannel){panel.style.display="none";panel.previousElementSibling.classList.remove("active");panel.parentElement.classList.remove("active");panel.previousElementSibling.querySelector('img').src='/assets/images/Down.svg'}});pannel.style.display="block";this.classList.add("active");this.parentElement.classList.add("active");img.src='/assets/images/Up.svg'}})});const smoothScroll=(target,duration)=>{const start=window.scrollY;const end=target.getBoundingClientRect().top+start;const distance=end-start;let startTime=null;const easeOutQuad=(t,b,c,d)=>{t/=d;return-c*t*(t-2)+b};const animation=currentTime=>{if(startTime===null)startTime=currentTime;const timeElapsed=currentTime-startTime;const run=easeOutQuad(timeElapsed,start,distance,duration);window.scrollTo(0,run);if(timeElapsed<duration)requestAnimationFrame(animation);};requestAnimationFrame(animation)};document.querySelectorAll('.footer-services a').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const targetId=this.getAttribute('href').substring(1);const targetElement=document.getElementById(targetId);if(targetElement){smoothScroll(targetElement,250)}})});document.querySelectorAll('.nav-links a').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const targetId=this.getAttribute('href').substring(1);const targetElement=document.getElementById(targetId);if(targetElement){smoothScroll(targetElement,250)}})})})